[project]
name = "airflow-orchestrate-weather-dashboard"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "apache-airflow==3.2.1",
    "jinja2>=3.1.6",
    "pandas>=3.0.3",
    "python-dotenv>=1.2.2",
    "requests>=2.34.0",
]

[project.scripts]
airflow-orchestrate-weather-dashboard = "airflow_orchestrate_weather_dashboard:main"

[build-system]
requires = ["uv_build>=0.11.13,<0.12.0"]
build-backend = "uv_build"
